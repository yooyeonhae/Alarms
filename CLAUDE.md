# CLAUDE.md

이 파일은 이 프로젝트 전체에서 Claude Code가 항상 지켜야 할 공통 규칙을 담는다.
특정 기능(예: 뉴스 브리핑)만의 세부 구현 규칙은 여기에 넣지 않는다. 해당 기능 폴더의 CLAUDE.md를 따로 둔다.

---

## 🔒 최우선 보안 규칙

**네이버 API 키(Client ID/Secret)를 프론트엔드 코드에 절대 넣지 않는다.**

- React 컴포넌트, 클라이언트 JS 번들, `.env`의 `VITE_`/`NEXT_PUBLIC_` 등 브라우저에 노출되는 환경 변수 어디에도 API 키 문자열을 두지 않는다.
- 외부 API(네이버 등) 호출이 필요하면 반드시 Supabase Edge Function 등 서버 사이드 환경에서만 키를 사용하고, 클라이언트는 그 서버 사이드 프록시만 호출한다.
- 커밋 전 diff에 API 키/Secret 문자열이 포함되어 있지 않은지 항상 확인한다. `.env` 등 실제 키가 담긴 파일은 커밋하지 않으며, 예시 파일에는 placeholder만 사용한다.
- 이 규칙은 다른 어떤 요구사항(속도, 편의성 등)보다 우선한다.

---

## 데이터 처리 원칙

- 외부 API로 조회한 데이터(예: 뉴스)는 DB에 저장하지 않고, 매 요청마다 실시간으로 다시 조회하는 것을 기본 원칙으로 한다.
- 캐싱/이력 저장이 필요하다고 판단되더라도, 별도로 명시적인 논의와 합의 없이 임의로 DB 저장 로직을 추가하지 않는다.

---

## 실패 허용(Fail-safe) 원칙

**부가 기능이 실패해도 알람의 핵심 기능(정해진 시각에 알람 소리를 재생하는 것)은 반드시 정상 동작해야 한다.**

- 뉴스 브리핑, 날씨 표시 등 알람 앱의 부가 기능에서 에러(네트워크 실패, API 오류, 타임아웃 등)가 발생해도 알람 소리 재생 로직에 영향을 주지 않도록 코드를 분리한다.
- 부가 기능의 예외는 해당 기능 범위 내에서만 처리하고, 알람 재생을 막거나 지연시키는 방식으로 전파되어서는 안 된다.
- 새 부가 기능을 추가할 때는 "이 기능이 통째로 실패해도 알람은 울리는가?"를 항상 자문한다.

---

## 작업 순서 원칙

새로운 기능을 구현할 때는 다음 순서를 따른다.

1. **화면(UI)을 가짜 데이터로 먼저 만든다.** 실제 API/백엔드 연결 없이 정적 목업 데이터로 화면 동작과 레이아웃을 먼저 완성한다.
2. **그 다음 백엔드를 연결한다.** UI가 확정된 뒤 실제 API 호출, 서버 사이드 로직을 연결한다.
3. **예외 처리는 마지막에 추가한다.** 정상 동작 흐름이 검증된 뒤에 에러 처리, 타임아웃, 폴백 UI 등을 추가한다.

이 순서를 건너뛰고 처음부터 에러 처리나 백엔드 연동을 함께 설계하지 않는다.

---

## 미확정 사항에 대한 원칙

각 기능의 PRD에 있는 Open Questions(예: 뉴스 브리핑 기능의 검색 키워드 방식, 자동/수동 재생 타이밍 등)는 아직 확정되지 않은 사항이다.

- Open Questions로 분류된 항목은 임의로 결정하거나 "이렇게 하는 게 맞겠지"라고 추측하여 구현하지 않는다.
- 구현 중 Open Question과 관련된 결정이 필요해지면, 먼저 사용자에게 확인을 구한 뒤 진행한다.

---

뉴스 기능의 세부 규칙(파일 구조, 함수 이름 규칙)은 news/CLAUDE.md를 참고할 것.
