# 최근뉴스 서비스 기술 명세서

## 1. 개요
알람 웹앱에 "최신 뉴스 5개 검색 및 읽어주기" 기능을 추가한다.
사용 기술 스택: **React + Supabase**

---

## 2. 핵심 기능
1. 최신 뉴스 5개 검색 및 리스트 표시
2. 각 뉴스를 음성으로 읽어주기 (TTS)
3. (선택) 전체 뉴스 자동 순차 재생

---

## 3. 기술 구성

### 3-1. 뉴스 검색 — Claude API (web_search 도구) + Supabase Edge Function
- 클라이언트(React)에서 직접 Anthropic API 키를 노출하지 않도록, **Supabase Edge Function**을 프록시로 사용한다.
  - React → Supabase Edge Function 호출 → Edge Function 내부에서 Claude API(`api.anthropic.com`) 호출 → 결과 반환
- Claude API 호출 시 `web_search` 툴을 포함하여 최신 뉴스를 검색한다.
- 프롬프트 설계 포인트:
  - "오늘의 주요 뉴스 5개를 JSON으로만 응답" 형태로 요청
  - 응답 스키마 예시: `{ title, summary(3문장 이내), source, url }[]`
  - 카테고리 필터(정치/경제/IT 등)가 필요하면 프롬프트에 카테고리 파라미터 추가
- 응답은 JSON만 받도록 시스템 프롬프트에 명시 (마크다운 코드블럭, 부가 설명 금지)

### 3-2. 데이터 저장 — Supabase Database
- 검색된 뉴스를 매번 실시간 검색하되, 캐싱 및 이력 관리를 위해 `news_cache` 테이블 운영 고려
  - 컬럼 예시: `id, title, summary, source, url, category, fetched_at`
- 사용자별 "읽은 뉴스" 여부, 즐겨찾기 등을 저장하려면 `user_news_status` 테이블 추가 고려
  - 컬럼 예시: `id, user_id, news_id, is_read, is_favorite`
- Row Level Security(RLS) 적용하여 사용자별 데이터 접근 제어

### 3-3. 뉴스 읽어주기 — 브라우저 내장 Web Speech API (SpeechSynthesis)
- 별도 서버/API 없이 브라우저 자체 TTS 기능 사용
- 한국어(`ko-KR`) 음성 지원, 재생/정지/속도 조절 구현 가능
- 개별 뉴스 카드에 "읽어주기" 버튼 배치
- 전체 자동 재생 시 5개 뉴스를 순서대로 이어서 낭독

### 3-4. 프론트엔드 — React
- 뉴스 카드 리스트 컴포넌트 (제목/요약/출처 표시)
- 카드별 "읽어주기" 버튼 + 재생 상태 관리 (useState)
- 전체 재생/정지 컨트롤
- 기존 알람 UI와 통일된 다크 테마 스타일 적용

---

## 4. 전체 흐름
1. 사용자가 "뉴스 브리핑" 버튼 클릭 (또는 알람 발생 시 자동 트리거)
2. React → Supabase Edge Function 호출
3. Edge Function → Claude API(`web_search` 툴 포함) 호출하여 뉴스 5개 JSON으로 수신
4. (선택) Supabase DB에 결과 캐싱
5. React에서 뉴스 카드 5개 렌더링
6. 사용자가 "읽어주기" 클릭 → SpeechSynthesis로 제목+요약 낭독
7. (선택) 전체 자동 재생 모드로 5개 순차 낭독

---

## 5. 고려 사항
- `web_search` 결과는 실시간 검색이므로 항상 최신성 보장됨
- TTS 부담을 줄이기 위해 뉴스 요약은 3문장 이내로 제한
- Anthropic API 키는 반드시 Supabase Edge Function(서버 사이드)에서만 사용, 클라이언트에 노출 금지
- 카테고리 필터, 즐겨찾기, 읽음 처리 등은 추후 확장 기능으로 분리 가능

---

## 6. 향후 논의 필요 항목
- UI 디자인: 화면당 표시 개수, 카드 스타일, 다크 테마 적용 방식
- 캐싱 정책: 뉴스 갱신 주기 (예: 1시간마다 재검색)
- 자동 트리거 여부: 알람 발생 시 자동으로 뉴스 브리핑 실행할지 여부
